package dtos;

public record EjercicioDTO(Long id, String nombre,String descripcion, int series, int repeticiones) {
    // Constructor generado automáticamente para el record

    // No es necesario implementar getters; se generan automáticamente
}
